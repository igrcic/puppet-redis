# !!! generated by puppet !!!

# Rotate redis sentinel log with standard logrotate.

<%= @redis_log_dir %>/<%= @sentinel_instance_name %>.log {
 weekly
 missingok
 copytruncate
 compress
 notifempty
 rotate 12
}
